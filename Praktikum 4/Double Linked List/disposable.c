/*    cout << "Masukkan Data : ";
    cin >> newDat;
    TNode *baru;
    baru = new TNode;
    baru->data = newDat;
    baru->next = NULL;
    baru->prev = NULL;

    if(isEmptyH() == true){
        head = baru;
        head->next = NULL;
        head->prev = NULL;
    }
    else{
        baru->next = head;
        head->prev = baru;
        head = baru;
    }
    cout << "Data \""<<newDat<<"\" berhasil dimasukkan di bagian depan.";
*/
